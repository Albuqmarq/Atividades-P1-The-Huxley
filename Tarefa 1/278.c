#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int valor1, valor2, valor3,valor4;
    scanf("%d %d %d %d", &valor1, &valor2, &valor3, &valor4);
    printf("DIFERENCA = %d\n", (valor1 * valor2)-(valor3 * valor4));
    
	return 0;
}
